{% extends 'base.html.twig' %}

{% block title %}Annonce : {{ post.title }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <!-- Colonne Gauche : Images -->
        <div class="col-md-7">
            <div class="card shadow-sm">
                <div class="card-body">
                    {% if post.media|length > 0 %}
                        <div id="carouselExample" class="carousel slide" data-bs-ride="carousel">
                            <div class="carousel-inner">
                                {% for image in post.media %}
                                    <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                                        <img src="{{ asset('uploads/' ~ image.url) }}" class="d-block w-100 rounded" alt="Photo de l'annonce" style="height: 400px; object-fit: cover;">
                                    </div>
                                {% endfor %}
                            </div>
                            {% if post.media|length > 1 %}
                                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
                                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                </button>
                                <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                </button>
                            {% endif %}
                        </div>
                    {% else %}
                        <div class="bg-light d-flex align-items-center justify-content-center rounded" style="height: 400px;">
                            <p class="text-muted">Aucune photo disponible</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Colonne Droite : Infos -->
        <div class="col-md-5">
            <h1 class="display-5 fw-bold">{{ post.title }}</h1>
            <hr>
            <p class="lead text-muted">{{ post.description }}</p>
            
            <div class="d-grid gap-2 mt-4">
                <button class="btn btn-success btn-lg" type="button">Contacter le propriétaire</button>
                <div class="d-flex justify-content-between mt-3">
                    <a href="{{ path('app_post_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
                    <a href="{{ path('app_post_edit', {'id': post.id}) }}" class="btn btn-warning">Modifier</a>
                </div>
                <div class="mt-3">
                    {{ include('post/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>

{# Ajout du CSS/JS Bootstrap pour le carrousel #}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

{% endblock %}